# aerich 迁移工具入口配置
# 连接信息读取自环境变量（common/config.py 中的 Settings），
# 因此此处仅声明模块与迁移目录，不在文件内硬编码账号密码。

[aerich]
# Tortoise ORM 初始化模块（内部执行 Tortoise.init 并注册全部模型）
tortoise_orm = common.config.TORTOISE_ORM
# 迁移脚本输出目录
location = ./migrations
# 源码根目录（相对 cwd）
src_folder = .
