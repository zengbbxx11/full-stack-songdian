# 后台修改与 GitHub 发布准备审查 — 2026-09-22

## 结论

当前本地版本可整理提交并推送 GitHub，未发现仍阻塞推送的确定性功能或构建问题。服务器更新应在该提交的 GitHub CI（含镜像、PostgreSQL、Lighthouse）成功后进行。本轮没有 commit、push 或服务器操作。

## 审查范围

以当前工作区相对 HEAD 的真实内容差异为主（忽略 Windows 换行噪声），重点审查相册创建、父级变更、防环校验、排序接口与媒体树、新闻 HTML 编辑、图片插入、沙箱预览，以及官网既有修改的构建和浏览器回归。没有修改业务 API 或数据库结构。

## 本轮修复

- 相册排序：同步锁与保存中禁用交互，避免同一页面连续操作发出交错的排序请求；完成服务端回读后再允许下一次排序。增加请求挂起期间的回归断言。
- 相册操作：桌面键盘焦点可显示操作按钮，无 hover 的触摸设备也可访问；展开按钮支持空格。
- 新闻图片 HTML：正确转义 URL 与 alt 中的属性字符，保留引号、& 等文字，避免拼接破坏 HTML。
- 沙箱预览：成功恢复后清除之前的错误状态；sandbox 与 CSP 隔离保持。
- 测试稳定性：首页新闻测试保留已找到夹具的页面，避免再次导航被其他并行用例创建的新闻挤出首页；产品生命周期测试只定位可见表单，避免 Next 隐藏表单副本触发严格定位错误。
- 发布清理：停止跟踪 5 个运行日志，本地文件保留；补充官网开发日志忽略规则。

## 实际验证

| 项目 | 结果 |
| --- | --- |
| 官网 ESLint、TypeScript、生产 build | 通过；最终测试调整后再跑定向 lint 与完整 typecheck 通过 |
| 后台 ESLint、TypeScript、生产 build | 通过，后台修复后再次通过 |
| 后端 Ruff | 通过 |
| 后端 pytest | 173 passed，6 个第三方弃用警告 |
| 浏览器全量回归 | 118 项执行；最后一轮 117 通过，1 个产品生命周期定位失败 |
| 修复后的相关浏览器回归 | 15/15 通过，包含上述失败用例、新闻预加载、相册排序与新闻编辑 |
| SEO/GEO 合约检查 | 通过 |
| 官网/后台生产依赖 audit | 均 0 vulnerabilities，使用 registry.npmjs.org；未永久修改镜像配置 |
| package 与 lock 根依赖版本 | 两项目一致 |
| Compose 配置与部署脚本语法 | 通过，使用 .env.example 验证 |

首两轮全量测试还暴露了新闻预加载用例的数据竞争，已定位并修复。没有把历史失败隐去，也没有宣称最后修改后重跑了全部 118 项。

本地浏览器回归跳过唯一会向真实邮件配置提交询盘的用例；询盘前端可靠性测试与后端隔离测试仍已执行。

## 发布边界及后续

- 本机 Docker daemon 未运行，没有实际构建/启动 Linux 容器；本轮未跑 Lighthouse 或 PostgreSQL 迁移集成任务，交由 GitHub CI。
- 当前部署工作流是 workflow_dispatch，普通 Git push 不会自动执行服务器部署；master 推送成功并通过 CI 后会构建镜像。
- 提交必须包含新增 ArticlePreviewFrame.tsx、article-html.ts、相册 E2E 文件及 package-lock.json，不能只提交已跟踪文件。
- 本地 backend/.env、admin-next/.env.local、frontend/.env.local 均被忽略。已跟踪文件的有限密钥签名扫描未发现私钥、GitHub token、AWS access-key 模式；这不代表全面的历史泄漏审计。
- 已跟踪文件没有超过 50 MiB 的单文件。大量状态变化来自 CRLF；本轮没有批量覆盖或重置原有工作。
- 删除日志跟踪仅影响下一次提交，不清除 Git 历史中的旧日志。
- 服务器发布沿用现有备份、迁移、健康检查流程；GitHub 保存代码，不会自动同步本地数据库或媒体内容。

## 后续修复：GitHub Contact Lighthouse 失败 — 2026-09-23

- GitHub 截图显示 `contact` 页面 accessibility 为 0.88，CI 门槛为 0.90。
- 修复联系方式 definition list 的非法嵌套、补足表单与页脚辅助文字对比度、纠正页脚标题级别，并扩大 Cookie Settings 触摸目标。
- 重新运行生产 `next build` 成功；用同一安装版本的 Lighthouse 在独立生产服务检查 `/contact`，accessibility 得分 **1.00**，没有未通过的无障碍审计项。
- 此次本地构建时没有启动后端，页面设置接口连接失败并走了回退值；无障碍评分仍是完整生产页面的 1.00。GitHub 的 CI 模拟 API 构建会再验证最终提交。
